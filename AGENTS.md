# L-suan 项目执行规则

本文件是仓库级规则，优先于个人全局默认规则。所有工作仍须遵守系统、开发者、安全规则和用户当次明确授权。

## 1. 主方案

处理知识库、学习路线、Demo、动画、视频素材或个人 IP 工作前，必须完整阅读：

1. `AI_FULLSTACK_LEARNING_IP_MASTER_PLAN.md`；
2. `planning/README.md`；
3. `planning/progress.md`；
4. 当前版本对应的 `planning/versions/vN.md`。

不得仅凭聊天记忆推翻主方案。重要新决策必须写回主方案或版本文件。

发生冲突时按以下顺序解释，且实际文件证据优先于进度声明：

1. 当前生效的上级指令、用户当次明确指令和安全规则；
2. 本项目 `AGENTS.md`；
3. `AI_FULLSTACK_LEARNING_IP_MASTER_PLAN.md`；
4. 当前 `planning/versions/vN.md`；
5. `planning/README.md`；
6. `planning/progress.md`。

## 2. 简短版本命令

用户发送以下简短命令时，将其视为对应版本的项目内开发授权：

- `开始第N版`
- `继续第N版`
- `开始 V N`
- `继续 V N`

其中 `N` 只能是 1～6。

### 2.1 “开始第N版”必须执行

1. 读取主方案、共享执行协议、进度文件和 `vN.md`；
2. 检查 Git 分支、工作区、项目结构和实际完成状态；
3. 用实际文件校正进度记录，不重复已完成工作；
4. 确认当前版本的目标、排除范围、模块顺序和验收标准；
5. 检查版本前置门槛；前置版本未达到“学习交付完成”时停止并报告，除非用户明确授权跳版；
6. 从第一个未完成模块开始，持续完成安全且已授权的工作；
7. 每次只激活一个完整知识模块；
8. 到达用户学习、独立编码、配音、目标学习者验证或其他必须由用户参与的检查点时暂停；
9. 持续更新 `planning/progress.md`，重大范围变化同时更新主方案和版本文件；
10. 分别记录“学习交付完成”和“教学验证完成”，不得混为一个状态。

### 2.2 “继续第N版”必须执行

按上述流程重新核实状态，从进度文件和实际产物确定的第一个未完成检查点继续。不得因为进入新对话而重做已验证完成的模块。

如果命令中的版本与 `planning/progress.md` 当前版本不一致，先核对用户是在切换、跳版还是输入错误；不得静默改变当前版本。

### 2.3 简短命令默认不授权

- Git 提交、推送、合并、PR、变基或历史重写；
- 创建、切换或删除分支；
- 正式部署、发布或代表用户向外部平台发内容；
- 购买域名、服务器、API 或其他服务；
- 读取、创建、修改或显示真实敏感值；
- 修改工作区之外的本机配置；
- 安装全局工具、系统服务或新增生产依赖；
- 连接或修改生产数据及外部系统。

如完成当前模块确需上述动作，先说明准确目标、原因、影响、风险、替代方案和恢复方式，等待本次明确授权。

## 3. 模块执行规则

- 学习优先，采用产品驱动的螺旋学习；
- 每个模块只能有一个核心问题和一个主要验证载体；
- 延伸知识进入后续队列，不得无限扩大当前模块；
- 动画和 Demo 按 A/B/C 教学价值分级；
- 核心模块必须包含用户独立编码、设计解释和预设故障排查；
- Codex 先提供需求、验收测试和分级提示，不立即提供可照抄答案；
- 独立练习使用隔离目录；用户通过前不得创建或展示参考实现；
- 用户本人通过后才能标记 Beta；
- 核心模块至少由一名目标学习者通过迁移任务，才能标记“教学验证通过”；
- 算法知识优先链接现有算法库，不重复编写；确有缺口时再补充；
- 网站动画、Demo、竖版视频、配音稿和发布素材遵循主方案中的职责边界。
- 课程事实优先引用官方文档、标准、规范或原始论文，并记录验证日期与版本；
- 首次外部发布前必须完成代码、内容/媒体、品牌和第三方素材的许可证声明。

## 4. 前端与可靠执行

- 任何前端创建、修改、重构或审查必须先加载并遵循 `liao-skills:ai-frontend-dev-rules`；
- 任何代码、文件、配置、数据或复杂多步骤变更必须先加载并遵循最新版 `liao-skills:reliable-task-execution`；
- 涉及 Git、外部写操作、依赖安装、敏感信息、部署或其他高风险动作时，先读取并遵守 `~/.codex/SECURITY_AGENT_RULES.md`；
- 项目内前端功能必须按项目现有结构拆分，入口文件保持组合职责；
- 修改后运行与风险相称的构建、内容、链接、示例和真实用户路径验证。

## 5. 进度真实性

- `planning/progress.md` 是跨对话进度入口，但不能替代实际文件检查；
- 不得把“已生成内容”写成“用户已掌握”；
- 不得把“用户已学习”写成“目标学习者已验证”；
- 不得把部分模块完成写成整个版本完成；
- 每次交付明确区分已完成、待用户、未验证和风险。
