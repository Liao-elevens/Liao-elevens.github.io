# 内容贡献规范

这个仓库同时维护个人博客、算法知识库和四语言示例。新增内容时遵循以下规则。

## 新增算法知识

1. 先在 `docs/coverage.md` 登记主题；
2. 确认所属领域和前置知识；
3. 复制 `templates/algorithm-topic.md`；
4. 使用稳定、可读的英文文件路径；
5. 补齐通俗实例、模拟、伪代码和四语言实现；
6. 分析复杂度、边界与常见错误；
7. 把文章加入 `docs/.vitepress/config.mts` 的导航；
8. 运行内容检查和网站构建。

## 内容边界

- 知识库正文保存稳定、经过验证的解释；
- 学习过程、思考和踩坑放在 `docs/blog/`；
- 前端内容放在 `docs/frontend/`；
- 不在多篇文章中复制同一大段定义，优先链接到权威正文；
- 不把只含标题或目录的页面标记为“完成”。

## 四语言规范

- 先写统一伪代码，再写具体语言；
- 四种实现使用相同变量语义和算法边界；
- Java 注意 `int`/`long` 与容器类型；
- Python 说明切片、内置函数隐藏的复杂度；
- JavaScript 注意数字精度和排序比较器；
- C++ 注意值、引用、迭代器和整数范围。

## 提交前检查

```text
npm run check
```

检查包括静态网站构建、内部 Markdown 链接和核心文章的四语言代码块。
